[variables]
NIXPACKS_SPA_OUTPUT_DIR = "dist"
NODE_VERSION = "22"

[phases.install]
cmds = ["npm ci --omit=dev --no-audit --no-fund"]

[phases.build]
cmds = [
  "rm -rf node_modules/.cache || true",
  "npm ci --include=dev",
  "npm run build"
]

[start]
cmd = "node server.js"