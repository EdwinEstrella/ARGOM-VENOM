[variables]
NIXPACKS_SPA_OUTPUT_DIR = "dist"
NODE_VERSION = "22"

[phases.build]
cmds = [
  "npm ci --include=dev",
  "npm run build"
]

[start]
cmd = "echo 'App built successfully - serving static files'"